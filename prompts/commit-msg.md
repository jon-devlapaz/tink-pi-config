---
description: Write a commit message from the staged diff
---
Run `git diff --cached` and `git log --oneline -10` to learn the repo's commit style.

Write one conventional commit message: `type(scope): subject` in imperative mood, subject <= 72 chars, body bullets only for non-obvious context.

Output only the message in a fenced code block. Do not stage or commit anything.
